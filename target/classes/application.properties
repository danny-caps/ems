spring.application.name=EMS
# MySQL Database connection
spring.datasource.url=jdbc:mysql://localhost:3306/ems_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Danny@8490

# JPA / Hibernate configs
spring.jpa.hibernate.ddl-auto=update       
 # 'update' will create tables if they don't exist
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Optional: Automatically create the database if it doesn't exist (already in URL)
# 'createDatabaseIfNotExist=true' in URL handles this
